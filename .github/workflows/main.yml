name: Test

on:
  push:
    paths:
      - .github/workflows/main.yml
      - biome.json
      - tsconfig.json
      - package.json
      - bun.lockb
      - src/**/*.ts

jobs:
  test:
    uses: winstxnhdw/actions/.github/workflows/bun.yml@10339f0c31654e18c00d8c7256351ba78bcb2ef7 # main
    with:
      disable-formatter: true
      disable-test: true
      disable-build: true
