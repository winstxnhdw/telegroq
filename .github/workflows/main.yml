name: Test

on:
  push:
    paths:
      - .github/workflows/main.yml
      - biome.json
      - tsconfig.json
      - package.json
      - bun.lockb
      - src/**/*.ts

jobs:
  test:
    uses: winstxnhdw/actions/.github/workflows/bun.yml@4d514e9cc5ee4206069ba3216a5751f08938e355 # main
    with:
      disable-formatter: true
      disable-test: true
      disable-build: true
