name: Test

on:
  push:
    paths:
      - .github/workflows/main.yml
      - biome.json
      - tsconfig.json
      - package.json
      - bun.lockb
      - src/**/*.ts

jobs:
  test:
    uses: winstxnhdw/actions/.github/workflows/bun.yml@0832d473774c1447617374fce8cf68e75c50dd7f # main
    with:
      disable-formatter: true
      disable-test: true
      disable-build: true
